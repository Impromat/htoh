<%= simple_form_for [@task, @booking], url: task_bookings_path(@task) do |form| %>
  <%= form.input :description, as: :text, placeholder: "J'accepte de vous rapporter de la pasteque pour 10€", label: false %>
  <%= form.submit "Faire une proposition", class: "btn btn-primary" %>
<% end %>
